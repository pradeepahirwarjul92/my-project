01 - What are the major features of Java 8?
Ans - Lambda, Functional Interfaces, Streams, Default methods, Optional, Date & Time API, Method References, CompletableFuture.

02 - What is a Lambda Expression?
Ans - Anonymous function that enables functional programming and reduces boilerplate code.

03 - Why were Lambdas introduced?
Ans - To support functional programming and make code concise & readable.

04 - Syntax of Lambda Expression
Ans - (args) -> expression

05 - What is a Functional Interface?
Ans - Interface with exactly one abstract method.

06 - @FunctionalInterface annotation – purpose?
Ensures only one abstract method exists (compile-time check).

07 - Common functional interfaces

	I) Predicate<T> - Take one argument , return booleab
		boolean test(T t)  - filter

		Predicate<Integer> isEven= n -> n%2 ==0;
        System.out.println(isEven.test(2));


	II) Function<T, R> -  Takes one argument, returns a result
		R apply (T t) - stream().map(function)
		
		Function<String,Integer> lengthFunc = s -> s.length();
        System.out.println(lengthFunc.apply("java")); //4 
		

	III) Consumer<T> - Takes one argument, returns nothing (void)
		void accept(T t)  - forEach(consumer)
	 
		Consumer<String> print = s -> System.out.println(s);
        print.accept("Hello Java");
	 

	IV)	Supplier<T> - Takes no argument, returns a value
		T get() - Optional.orElseGet(supplier)
		
		Supplier<Date> dateSupplier = () -> new Date();
        System.out.println(dateSupplier.get());


08 - What is Stream API?
	Used to process collections in a functional, declarative way.

09 - Difference between Stream and Collection
	| Collection 		| Stream |
	|---------			|-------|
	| Stores data 		| Processes data |
	| Reusable 			| One-time use |
	| Eager 			| Lazy |

10 - Intermediate vs Terminal operations
Ans - Intermediate operations are lazy and return a Stream, while terminal operations trigger execution and produce a result.

		Intermediate: map(), filter()
		Terminal: collect(), forEach(), reduce()

11 - What is Lazy Evaluation?
Ans - Stream operations execute only when terminal operation is called.

12 - map() vs flatMap() with example.
Ans - map() is used when one input produces one output.
	  flatMap() is used when one input produces multiple outputs and we need to flatten them into a single stream. 
	
map() transforms each elements into exactly one output element. 
syntax - Stream<R> map(Function<T,R> mapper) 
List<String> names = Arrays.asList("ram","ramesh","shyam","mohan");
        
        List<String> upperNames =    names.stream()
                                            .map(name -> name.toUpperCase())
                                            .collect(Collectors.toList());
        
        System.out.println(upperNames);
        
        //find length 
        
        List<Integer> lengths =    names.stream()
                                        .map(String::length)
                                        .collect(Collectors.toList());
        
        System.out.println(lengths);
		
		OUTPUT - [RAM, RAMESH, SHYAM, MOHAN]
					[3, 6, 5, 5]

flatMap() - transforms each element into multiple elements 
		(Stream inside Stream) and then flattens them into a single Stream.
	
	syntax	- Stream<R> flatMap(Function<T,Stream<R>> mapper)
	
	  List<List<String>> list = Arrays.asList(
                Arrays.asList("Java","Spring"),
                Arrays.asList("Hibernate","JPA"),
                Arrays.asList("Docker","Kubernetes")
        );
        
      List<String> result =  list.stream()
                    .flatMap(innerList -> innerList.stream())
                    .collect(Collectors.toList());
      
        System.out.println(result); 
		
		OUTPUT - [Java, Spring, Hibernate, JPA, Docker, Kubernetes]

	Real Time Example - 
	/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package com.heg.test;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

/**
 *
 * @author softwaredeveloper
 */
class Student {
String name;
List<String> subjects;

    public Student(String name, List<String> subjects) {
        this.name = name;
        this.subjects = subjects;
    }

    public List<String> getSubjects() {
        return subjects;
    }

}
public class RealTimeExample {
    
    public static void main(String[] args) {
        List<Student> students = Arrays.asList(
                new Student("Ram", Arrays.asList("Math","Physics")),
                new Student("Shyam", Arrays.asList("Chemistry","Biology"))
        );
        
        
        List<List<String>> subjects = students.stream()
                .map(Student :: getSubjects)
                .collect(Collectors.toList());
        
        System.out.println(subjects);
        
        //Instend this using flatmap 
       List<String> allSubjects =  students.stream()
                                        .flatMap(e -> e.getSubjects().stream())
                                       // .map(s -> s.toUpperCase())
                                        .collect(Collectors.toList());       
        System.out.println(allSubjects);
        
        
        
    }
    
}






13 . What is Parallel Stream?
Ans - Stream that runs operations in multiple threads using ForkJoinPool.
	Parallel Stream is a Stream API feature that processes elements concurrently using the Fork/Join framework to utilize multi-core processors and improve performance.
	
14 . forEach() vs forEachOrdered() explain both case . 
Ans - forEach() does not guarantee order in parallel streams, while forEachOrdered() maintains encounter order but may reduce parallel performance.


15 . What is Optional?
Ans - Optional<T> is a container object introduced in Java 8 that may or may not contain a non-null value.
	  It is used to avoid NullPointerException (NPE).
Container to avoid NullPointerException.


16 . orElse() vs orElseGet()

Ans - orElse() → always executes
	orElseGet() → lazy execution

17. Can Optional be null?
Ans - ❌ No, Optional itself should never be null.

18 . Types of Method References

Static → Class::method

Instance → obj::method

Constructor → Class::new


19 . Difference between Lambda and Method Reference
Ans - Lambda creates behavior, while Method Reference reuses existing behavior.

20 . Why default methods introduced?
Ans - To add new methods to interfaces without breaking implementations.
	Default methods were introduced in Java 8 to allow adding new methods to interfaces without breaking existing implementations, 
	enabling backward compatibility and supporting functional programming features like the Stream API.

21 . Can default methods be overridden?
Ans - ✅ Yes

22 . Can interfaces have static methods?
Ans - ✅ Yes (Java 8 onwards)

23 . Problems with old Date API?
Ans - The old Date API had problems like mutability, lack of thread safety,
 confusing month indexing, poor timezone handling,
 deprecated methods, and bad design. 
 Java 8 introduced the java.time API to provide immutable, thread-safe, and well-designed date-time classes.

1. Not Thread-Safe
	SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd");
	If multiple threads use the same instance → ❌ unpredictable results.
	
	Reason : 
	- It is mutable
	- It maintains internal state 
	
👉 Causes race conditions in multithreading.

2 . Mutable Classes 
	Date date = new Date();
	date.setYear(120); //changes internal state 
	
	Mutable objects are dangerous because:
		- Hard to debug
		- Not thread-safe
		- Side effects occur

3.  Confusing and Poor Design
	Date date = new Date(2024, 1, 1);

	Problems : 
	 -	Year start from 1900
	 - Month start from 0
Very confusing.

4. Month Index Starts from 0

	Calendar cal = Calendar.getInstance();
	cal.set(2024, 0, 1);  // 0 means January
		❌ Not intuitive
		❌ Easy to make mistakes	
		
5. 	No Clear Separation of Concepts	
	Old API mixes everything:
		Date
		Time
		Timezone
		Formatting
	All in one messy structure.

	No clear distinction like:
		Date only
		Time only
		Date + Time
		Zoned time

6. 	Poor Timezone Handling 
		Handling timezones was complex:
			-	TimeZone tz = TimeZone.getTimeZone("Asia/Kolkata");
			-	Calendar cal = Calendar.getInstance(tz);
		Very verbose and error-prone.	

7. 	Hard to Perform Calculations
		Example: Add 10 days
			-	Calendar cal = Calendar.getInstance();
			-	cal.add(Calendar.DATE, 10);


			-	Not readable
			-	Too many constants
			-	Very procedural

8. 	Deprecated Methods
		Many methods in Date were deprecated because of bad design
			-	date.getYear();   // Deprecated
			-	date.getMonth();  // Deprecated

9. 	No Fluent API
		Old API is not readable.
			-	Calendar cal = Calendar.getInstance();
			-	cal.add(Calendar.MONTH, 1);
			-	cal.add(Calendar.DATE, 5);
	Hard to read compared to modern API.
	
	
	
24. How is new Date API thread-safe?
Ans - The Java 8 Date-Time API is thread-safe because its classes are immutable. They are final, contain private final fields, 
	and do not provide setters. All modification methods return new objects instead of changing existing ones, eliminating shared mutable 
	state and the need for synchronization.

25. Future vs CompletableFuture
Ans - Future = Basic async
	CompletableFuture = Advanced async + Functional style

		1 - Blocking vs Non-Blocking
				Future - 
					String result = future.get(); // blocks			
					Main thread waits.

				CompletableFuture
					future.thenAccept(result -> 
					System.out.println(result));
		
		2 - Chaining Example
		

Non-blocking callback.
		
	| Future 		| CompletableFuture |
	|---------------|----------------|
	| Blocking 		| Non-blocking |
	| No chaining 	| Supports chaining |
	| Limited 		| Powerful async |

26.  thenApply() vs thenCompose()
Ans - 	thenApply() → transforms result
		thenCompose() → flattens async calls
		
		“thenApply() is used when the callback returns a normal value, while thenCompose() is used when the callback itself returns another CompletableFuture, preventing nested futures.”

27 . Common Collectors

Ans - 	toList() 		→ Collect to List
		toSet()			→ Collect unique elements
		groupingBy()	→ Multiple groups
		partitioningBy()→ Two groups
		joining()		→ Convert to String
		
		groupingBy vs partitioningBy
			groupingBy → multiple groups
			partitioningBy → only two groups (true/false)
			


28 . Are streams faster than loops?
Ans - ❌ Not always. Streams focus on readability, not speed.

29 . Stateful lambda – good or bad?
Ans - ❌ Bad practice (thread-safety issues)

30 . Can streams modify original collection?
Ans - ❌ No

31. Can we reuse a stream?
Ans - ❌ No → IllegalStateException
	No, a stream cannot be reused after a terminal operation. Once a stream is consumed, it is closed, 
		and attempting to operate on it again results in IllegalStateException. To reuse logic,
		a new stream must be created from the source.
	
	Example - Stream<String> stream = Stream.of("Java","Spring","Boot");
			stream.forEach(System.out::println);
			// X Reusing same stream
			stream.count();
			//Netflix movie streaming 🎬	

32. Can lambda throw checked exception?
Ans - ❌ Not directly (must handle or wrap)

33 . Difference between findFirst() and findAny()?
Ans - 
	findFirst()

		-	Maintains encounter order
		-	Even in parallel, must respect order
		-	Slightly slower in parallel

	findAny()
	
		-	Does not care about order
		-	Optimized for parallel
		-	Can return any matching element

	
	findFirst → ordered
	findAny → faster in parallel

34. Filter even numbers using streams
Ans - List<Integer> evenList = 	list.stream().filter( n -> n%2 ==0).collect(Collectors.toList());

35. Find duplicate elements using streams
Ans -  import java.util.*;
import java.util.stream.*;

public class FindDuplicates {
    public static void main(String[] args) {

        List<Integer> numbers =
                Arrays.asList(1, 2, 3, 4, 2, 5, 3, 6);

        Set<Integer> seen = new HashSet<>();

        Set<Integer> duplicates =
                numbers.stream()
                       .filter(n -> !seen.add(n)) //keeps only duplicate elements because HashSet.add() returns false for values already present.
                       .collect(Collectors.toSet());

        System.out.println(duplicates);
    }
}


36 . Sort a list using lambda
Ans -   // Reverse Natural Order (Number/String) 
        List<Integer> naturalSortedOrderAsc = list.stream()
                .distinct()
                .sorted()
                .collect(Collectors.toList());
        System.out.println("naturalSortedOrderAsc  :: " + naturalSortedOrderAsc);

        List<Integer> naturalSortedOrderDesc = list.stream()
                .distinct()
                .sorted(Comparator.reverseOrder())
                .collect(Collectors.toList());
        System.out.println("naturalSortedOrderDesc :: " + naturalSortedOrderDesc);

37. Convert list to map using streams
Ans -    Map<String,Long> salaryDept = employees.stream()
                                        .collect(Collectors.groupingBy(
                                                Employee::getDepartment, 
                                                Collectors.summingLong(Employee::getSalary))
                                        );

38. Count frequency of characters in a string
Ans  Map<Character,Long> map1 =  str.chars()
                                        .mapToObj(c -> (char) c)
                                        .collect(Collectors.groupingBy(
                                                        Function.identity(),
                                                         Collectors.counting()
                                        ));
       
        System.out.println("map1:: " +map1);

39. Find max/min using streams
Ans -  int max = list.stream().mapToInt(Integer::intValue).max().orElse(0);
        System.out.println("max :: " + max);
		
		int min = list.stream().mapToInt(Integer::intValue).min().orElse(0);
        System.out.println("min :: " + min);
		

40. Why are lambda expressions stateless? What problems occur if they aren’t?
Ans - Lambda expressions are stateless because Java treats them as pure functions.
If they maintain state, it can cause race conditions, unpredictable results, debugging difficulty, and unsafe behavior especially in parallel streams.


41. Can a lambda access instance and static variables? Explain rules.
Ans - A lambda can access local, instance, and static variables.
Local variables must be effectively final, while instance and static variables can be freely read and modified because they live in heap/class memory rather than stack memory.

package challenge60.days.day04;

public class LambdaVariableAccessConcept {
	
	static int s =100;
	int i =50;
	public void test () {
		int local=10;
		
		Runnable r =() ->{
			System.out.println(local);
			System.out.println(i);
			System.out.println(s);
			i++;
			s++;
			// local++; //Local variable local defined in an enclosing scope must be final or effectively final
			System.out.println("---------------");
			System.out.println(local);
			System.out.println(i);
			System.out.println(s);
			
		};
		r.run();
	}
	
	public static void main(String[] args) {
		LambdaVariableAccessConcept obj = new LambdaVariableAccessConcept();
		obj.test();
	}

}


42. Why must local variables used inside lambda be effectively final?
Ans - Local variables in lambda must be effectively final because lambda captures a copy of the local variable, 
and allowing modification would create inconsistent behavior.

43. Difference between anonymous inner class vs lambda (memory & behavior)?
Ans - Anonymous inner class creates a separate class object, while lambda is a lightweight function object that uses less memory and shares the outer class context.

44. Can lambda expressions be serialized?
Ans - Lambda expressions can be serialized only when the target functional interface extends Serializable, 
because serialization depends on the functional interface, not the lambda syntax itself.

45. How does Java implement lambdas internally (invokedynamic)?
Ans - Java compiles lambdas into an invokedynamic bytecode instruction, and at runtime the JVM uses LambdaMetafactory to 
dynamically create an implementation of the functional interface.

46. Can you overload methods with functional interfaces?
And -Methods can be overloaded with functional interfaces, but if multiple interfaces have compatible abstract methods, 
a lambda expression may cause an ambiguous method call unless explicitly cast.


54. Can functional interfaces extend other interfaces?
Ans - Invalid '@FunctionalInterface' annotation; Child is not a functional interface
package challenge60.days.day04;

@FunctionalInterface
interface Parent{
	void show();
}

@FunctionalInterface
interface Child extends Parent{
	void show1();
}

public class FunctionalInterfaceDemo {
	 public static void main(String[] args) {

	        Child c = () -> System.out.println("Child is also functional interface");

	        c.show();
	    }
}



55. Why are streams lazy?
Ans - Streams are lazy to improve performance and memory usage by delaying execution until a terminal operation is invoked, 
allowing Java to process only the required data efficiently.

56. What happens internally when a stream pipeline executes?
Ans - 
1. Create source
2. Build pipeline
3. Terminal starts execution
4. Process one element at a time
5. Pass through each stage
6. Produce result

57. Why are streams not stored anywhere?
Ans - Streams are not stored because a stream is not a data structure; it is a lazy pipeline that processes elements from the source 
on demand without creating intermediate storage.

58. How does short-circuiting work (findFirst, anyMatch)?
Ans - Short-circuiting works by allowing terminal operations like findFirst() or anyMatch() to stop the stream traversal as soon as 
the result is determined, using internal cancellation to prevent processing remaining elements.

63. Real-world use case for flatMap()?
Ans - A real-world use case of flatMap() is flattening nested collections, such as extracting all orders 
	from multiple customers into a single list for processing or reporting.


66. What happens if flatMap() returns null?
And - If flatMap() returns null, Java throws a NullPointerException because flatMap() expects every mapper function to return a 
valid Stream, even if it is an empty stream.

68. What is the ForkJoinPool?
Ans - 
The ForkJoinPool is a special thread pool in Java designed to efficiently execute tasks that can be:

split into smaller subtasks (fork)
processed in parallel
combined later (join)

It is part of Java's concurrency framework.

I. Functional Interfaces & Lambda Internals
JVM Mechanics: How does Java 8 reduce anonymous class overhead using invokedynamic, and are lambdas cached or stored in a specific memory area?

State & Lifecycle: Distinguish between stateless and stateful lambdas. How do escape analysis and GC performance benefit from the lambda architecture?

Method Reference Nuances: Compare Class::instanceMethod vs. obj::instanceMethod. In what scenarios does a lambda work where a method reference fails (e.g., ambiguity or generics)?

Constructor References: How do constructor references interact with generics and overloading?

II. Stream API: Mechanics & Performance
Data Structures: Why is a Stream not considered a data structure, and why do parallel streams perform poorly with LinkedList compared to ArrayList or arrays?

Pipeline Fusion: Explain stream pipeline fusion. How does it optimize performance, and what happens if an exception occurs mid-pipeline?

Infinite Streams: How can a stream be infinite, and what are the behavioral risks when using limit() or distinct() on an infinite parallel stream?

Side Effects & Safety: Why is peek() discouraged for logic, and why does shared mutable state break the thread-safety of parallel streams?

Spliterator Mastery: What is a Spliterator, how do its characteristics() affect optimization, and how does trySplit() manage the decomposition of parallel tasks?

III. Advanced Collectors & Reductions
Collect vs. Reduce: Explain the fundamental difference between collect() and reduce(). Why is reduce() considered unsafe when used with mutable objects?

Grouping & Partitioning: Compare groupingBy() vs. groupingByConcurrent() and explain why partitioningBy is more efficient than groupingBy for boolean keys.

Custom Collector Architecture: Describe the internal roles of the supplier, accumulator, and combiner. Why is a correct combiner implementation vital for parallel streams?

Map Handling: How does joining() work for strings internally, and how do you handle duplicate key collisions in toMap()?

IV. The Optional Pattern
Design Philosophy: Why was Optional not added to collections, and why is it discouraged as a class field, method parameter, or setter?

Functional Usage: Compare map() vs. flatMap() in Optional. Why are orElseGet() and orElseThrow() preferred over the "dangerous" get()?

Performance & Serialization: What is the performance impact of Optional vs. null, and why is Optional not Serializable by default?

V. CompletableFuture & Asynchrony
Chaining & Composition: Distinguish between thenApply(), thenAccept(), and thenRun(). When should you use thenCompose() versus thenCombine()?

Error & Timeout Management: How does exception handling work in a CompletableFuture chain, and how do you implement timeouts?

Resource Management: What is the default thread pool for CompletableFuture, and why is blocking (e.g., DB/REST calls) inside it dangerous for the rest of the application?

Parallel Streams vs. CompletableFuture: When should you choose one over the other for complex async workflows?

VI. Modern Date/Time & Interface Changes
Immutability & Design: Why is the New Date API (java.time) immutable? Contrast Instant vs. LocalDateTime regarding time-zone handling.

Legacy Migration: How do you safely convert a legacy java.util.Date to modern types like LocalDateTime?

Interface Evolution: How does Java 8 solve the Diamond Problem with default methods, and why can these methods not override Object methods like equals() or hashCode()?

Concurrency in Interfaces: Can default methods be synchronized, and how do you explicitly invoke a specific interface's default implementation?

VII. General Best Practices
The "When Not To Use" Rule: Under what specific architectural conditions should you avoid Java 8 features (Streams/Lambdas) entirely in favor of traditional loops or patterns?


1 - why use mapToInt
Ans - mapToInt()is intermediate stream operation
		use for convert a Stream of objects into a primitive stream
		mainly used when performing numerice operations sum, average, max, or min 
		
2 - difference between  .mapToInt(Integer::parseInt)
						.mapToInt(Integer::intValue)	
Ans - 	
	Feature 	parseInt 		intValue	
	Input Type 	String			Integer
	Conversion	String->int 	Integer ->int
	Used For 	Parsing text	Removing boxing
	
	mapToInt(Integer::parseInt)
	

3 -
Ans -  	

1. What is Stream API in Java 8?
Ans - I use Stream to write clean, declarative, and maintainable code for bulk data operation like filtering, transaformation, aggregation, and grouping - 
without explict iteration . 
	Technically, Stream works on pipeline model : 
	  Source - collection.stream()
	  Intermediate operation(Lazy) - filter(), map(), sorted(), distinct()
	  Terminal operations - collect(),reduce(),count(),foreach

 Key points I usually highlight :
	# Lazy evaluation improves performance
	# Support parallel processing using parallelStream()
	# Encourages immutability and side-effect-free logic
	# Works well with Collectors for grouping, partitioning, summarizing 
	

2. 	Difference between Collection and Stream?
Ans - # Collection
		. Used to store data in memory.
		. Supports CRUD operations (add, remove, update).
		. Uses external iteration (for-loop, iterator).
		. Data is available immediately.
		. Can be iterated multiple time
		
	  # Stream
		. Used to process data, not store it.
		. Supports functional-style operations like filter, map, reduce.
		. Uses internal iteration.
		. Follows a pipeline model (lazy intermediate operations + terminal operation).
		. Cannot be reused once consumed.

3. 	What is the difference between intermediate and terminal operations?	
Ans - 	Intermediate operations are lazy and optimized — operations are processed in a single pass when terminal operation is called.
		Terminal operation is mandatory; without it, stream does nothing.
		After terminal operation, the stream cannot be reused.
	
4. What happens if no terminal operation is used?
Ans 	Without a terminal operation, stream pipeline is not executed.
		No data processing happens.
		No exception is thrown.
		Stream behaves like a blueprint until a terminal operation is called.
		
5. What is lazy evaluation in Streams?
Ans - 	Lazy evaluation means intermediate operations in a stream are executed only when a terminal operation is called, 
allowing optimized and efficient single-pass data processing.	

6. Can a Stream be reused after a terminal operation?
Ans - A Stream cannot be reused after a terminal operation because it gets consumed. To process the data again, a new Stream must be created from the source.

7. What is the difference between map() and flatMap()?
Ans - map() is used for simple transformation of elements, whereas flatMap() is used when the transformation produces nested streams and we need to flatten them 
into a single stream.

8. What is the difference between filter() and distinct()?
Ans - filter() is used for conditional filtering based on business logic, whereas distinct() is specifically used to eliminate duplicate elements using equals and hashCode.

9 . What is peek() used for?
Ans -  
🔹 Purpose of peek()
	Performs an action on each element
	Does not modify the stream
	Mainly used for logging / debugging
	Lazy — executes only when terminal operation is called


 List<Integer> list = Arrays.asList(10,2,6,8,4,85,0,45,6,4,7,4,1,5,12);
    List<Integer> peekDemo =   list.stream()
               .filter(n -> n >10)
               .peek(n -> System.out.println("After filter::" +n))
               .map(n -> n*2)
               .collect(Collectors.toList());
    
        System.out.println("peek demo :: " +peekDemo);
		
10. What is the difference between forEach() and forEachOrdered()?
Ans - 	forEach() focuses on performance and may not preserve order in parallel streams, whereas forEachOrdered() guarantees encounter order even in parallel processing, 
with a possible performance trade-off.	

11 - What is Collectors class?
Ans -	The Collectors class in Stream API (introduced in Java 8) is a utility class that provides factory methods to collect 
		stream elements into different forms like List, Set, Map, or perform grouping, partitioning, and aggregation.		
		It is mainly used with the collect() terminal operation.
		
12 - What is the default Map implementation used by groupingBy()?
Ans - By default, groupingBy() uses a HashMap, but we can provide a custom Map supplier 
			like LinkedHashMap or TreeMap if ordering or sorting is required.
		
	🔥 Important Points
			Default Map → HashMap
			Order is not guaranteed

13 - How to change the Map type in groupingBy()?
Ans - To change the Map type in groupingBy(), we use the overloaded method 
	that accepts a Map supplier, allowing us to use LinkedHashMap, TreeMap, or 
	any custom Map implementation as per requirement.
	
	🔥 Why This Works
			Internally, groupingBy():
				Uses the provided Supplier to create the Map.
				Accumulates values per key.
				Uses combiner for parallel streams.
			
14 - What is a downstream collector?	
Ans - A downstream collector is a collector that is applied after grouping or partitioning, to further process the grouped elements.
	Internally

🔥 Internally
	groupingBy(classifier, downstreamCollector)
		First → classify elements by key
		Then → apply downstream collector on each group
		Instead of List<T>, you get aggregated or transformed result

15 - Difference between groupingBy() and partitioningBy()?
Ans - partitioningBy() is a specialized version of groupingBy() for boolean conditions, 
always producing two groups, while groupingBy() is more flexible and can create 
multiple groups based on any classification function.	

16 - What is counting() collector?
Ans - counting() is a downstream collector in Java 8 Stream API that counts the number of elements in a stream or within each group.
	counting() is a downstream collector used to count elements in a stream or within grouped results, returning the count as a Long value.	
	
17 - Difference between summingLong() and mapToLong().sum()?
Ans - 	1 - summingLong()
			- It is a Collectors
			- Used inside collect()
			- Mostly used as a downstream collector (e.g. with groupingBy)
	
		2 - mapToLong().sum()
			-	Uses primitive stream (LongStream)
			- 	Terminal operation
			-	More direct and slightly more efficient
	
	🔥 Key Differences
	summingLong()						mapToLong().sum()
	Collector							Terminal operation
	Used inside collect()				Direct sum on primitive stream
	Good for grouping/aggregation		Good for simple summation
	Slight boxing overhead				More performance optimized
	
18 - 	What is collectingAndThen()?
Ans - collectinAndThen() is used to apply a final transformation after a collector finished its operation, 
	enabling post-processing of collected results within the same stream pipeline.
	
	🔥 Why it's Used
	- When result of collector need post-processing
	- Help avoid extra stream operation
	- Keep logic inside collect() pipeline


19 - How to make grouping result immutable?
Ans - To make the grouping result immutable, we use collectingAndThen() to wrap the result with an unmodifiable collection.	
	
		 Map<String,List<Employee>> immutableList = employees.stream()
                .collect(Collectors.collectingAndThen(
                        Collectors.groupingBy(Employee::getDepartment), 
                        Collections::unmodifiableMap
                ));
				
20 -	Difference between Comparable and Comparator?
Ans - 	🔥 Real-World Understanding
		Use Comparable when class has a single natural order (e.g., Employee by ID).
		Use Comparator when sorting varies (by salary, name, date, etc.).
			
		Comparable defines the natural ordering of a class internally using compareTo(), 
		while Comparator allows multiple custom sorting strategies externally using compare(), 
		providing greater flexibility.	
	
21 - What is Comparator.comparing()?
Ans - Comparator.comparing() is a Java 8 utility method that creates a Comparator 
	based on a key extractor function, making sorting more concise, readable, 
	and flexible compared to traditional Comparator implementations.

22 - What is Comparator.comparingLong()?
Ans - Comparator.comparingLong() is used to create a comparator based on a long 
primitive field, avoiding boxing overhead and providing better performance compared 
to Comparator.comparing() when dealing with primitive long values.	
	
23 - How to reverse sorting using streams?
Ans - in java8, we can reverse sorting in stream using : 
	# Comparator.reverseOrder()
	# .reversed() method
	# Custom comparator
	

24 - How to sort by multiple fields?
Ans - 	In Java 8, multi-level sorting is achieved using Comparator.comparing() followed by thenComparing(), 
allowing clean, readable, and flexible sorting logic across multiple fields.

